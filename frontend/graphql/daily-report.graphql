query GetDailyTherapeutics($userId: ID!, $today: DateTime!) {
    medicineAssignments(where: {
        user: { id: { equals: $userId } },
        startDate: { gte: $today },
        endDate: { lte: $today },
    }) {
        directions
    }
}
